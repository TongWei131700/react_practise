function NavItem(props){
  return (
    <>
    <button onClick={props.onClick}>Click Me</button>
    <!--
{`function NavItem(props){
  return (
    <button onClick={props.onClick}>Click Me</button>
  )
}

export default NavItem;`}
    -->
    </>
  )
}
export default NavItem;
